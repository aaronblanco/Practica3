<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="ExtraText"
    package="com.example.practica3">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Nuevo: permiso de ubicación precisa -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_background_shopping"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_background_shopping"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practica3"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config" >

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Practica3">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".CartActivity"
            android:label="Carrito de Compras">
        </activity>
        <activity
            android:name=".CheckoutActivity"
            android:label="Tramitar Pedido">
        </activity>
        <activity
            android:name=".AddProductActivity"
            android:label="Añadir Producto">
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="Iniciar Sesión">
        </activity>
        <activity
            android:name=".MapActivity"
            android:label="Dónde estamos">
        </activity>
    </application>


</manifest>